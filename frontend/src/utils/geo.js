// Deep Micro-Locality Database across Ghana (Landmarks, Streets, Suburbs, Municipalities)
export const GHANA_MICRO_LOCALITIES = [
  // Oyibi & Greater Accra Micro-Localities
  { name: 'Valley View University Road, Oyibi', suburb: 'Oyibi', district: 'Kpone Katamanso / Ga East', lat: 5.795, lng: -0.133 },
  { name: 'Oyibi Junction, Accra-Dodowa Road', suburb: 'Oyibi', district: 'Kpone Katamanso', lat: 5.790, lng: -0.130 },
  { name: 'Dodowa Central Market Area', suburb: 'Dodowa', district: 'Shai Osudoku', lat: 5.883, lng: -0.093 },
  { name: 'Adenta Barrier / Shopping Mall Area', suburb: 'Adenta', district: 'Adentan Municipal', lat: 5.715, lng: -0.160 },
  { name: 'Boundary Road, East Legon', suburb: 'East Legon', district: 'Ayawaso West Municipal', lat: 5.638, lng: -0.152 },
  { name: 'Madina Zongo Junction', suburb: 'Madina', district: 'La Nkwantanang-Madina', lat: 5.665, lng: -0.168 },
  { name: 'Atomic Junction, Haatso Road', suburb: 'Haatso / Legon', district: 'Ga East Municipal', lat: 5.658, lng: -0.185 },
  { name: 'Oxford Street, Osu RE', suburb: 'Osu', district: 'Klottey Korle', lat: 5.556, lng: -0.182 },
  { name: 'Spintex Coastal Estate Junction', suburb: 'Spintex', district: 'Ledzokuku Municipal', lat: 5.623, lng: -0.098 },
  { name: 'Kwame Nkrumah Interchange (Circle)', suburb: 'Adabraka / Circle', district: 'Klottey Korle', lat: 5.559, lng: -0.208 },
  { name: 'Dansoman Keep Fit Club Area', suburb: 'Dansoman', district: 'Ablekuma West', lat: 5.548, lng: -0.262 },
  { name: 'Kaneshie First Light Junction', suburb: 'Kaneshie', district: 'Okaikwei South', lat: 5.568, lng: -0.238 },
  { name: 'Lapaz Bambolino Area', suburb: 'Lapaz', district: 'Okaikwei North', lat: 5.593, lng: -0.246 },
  { name: 'Achimota Retail Centre Area', suburb: 'Achimota Dome', district: 'Ga East Municipal', lat: 5.618, lng: -0.228 },
  { name: 'Pokuase Interchange Area', suburb: 'Pokuase', district: 'Ga North Municipal', lat: 5.680, lng: -0.290 },
  { name: 'Airport City, Liberation Road', suburb: 'Airport Residential', district: 'Ayawaso West', lat: 5.602, lng: -0.177 },
  { name: 'Cantonments Post Office Road', suburb: 'Cantonments', district: 'La Dade Kotopon', lat: 5.576, lng: -0.172 },
  { name: 'Labone Coffee Shop Area', suburb: 'Labone', district: 'La Dade Kotopon', lat: 5.564, lng: -0.163 },
  { name: 'Tema Comm. 1 Market Square', suburb: 'Community 1', district: 'Tema Metropolitan', lat: 5.642, lng: 0.002 },
  { name: 'Tema Comm. 25 Mall Area', suburb: 'Community 25', district: 'Kpone Katamanso', lat: 5.712, lng: 0.083 },
  { name: 'Ashaiman Market Square', suburb: 'Ashaiman', district: 'Ashaiman Municipal', lat: 5.698, lng: -0.035 },
  { name: 'Kasoa New Market Area', suburb: 'Kasoa', district: 'Awutu Senya East', lat: 5.532, lng: -0.418 },

  // Kumasi / Ashanti Deep Localities
  { name: 'Adum Post Office Road', suburb: 'Adum', district: 'Kumasi Central', lat: 6.691, lng: -1.622 },
  { name: 'KNUST Commercial Area / Library', suburb: 'KNUST Campus', district: 'Oforikrom Municipal', lat: 6.675, lng: -1.569 },
  { name: 'Bantama High Street', suburb: 'Bantama', district: 'Bantama Sub-Metro', lat: 6.702, lng: -1.636 },
  { name: 'Asokwa Mall Area', suburb: 'Asokwa', district: 'Asokwa Municipal', lat: 6.669, lng: -1.604 },
  { name: 'Suame Magazine Zone 1', suburb: 'Suame', district: 'Suame Municipal', lat: 6.722, lng: -1.629 },
  { name: 'Ayigya Zongo, Kumasi', suburb: 'Ayigya', district: 'Oforikrom Municipal', lat: 6.682, lng: -1.558 },
  { name: 'Tafo Mile 4 Junction', suburb: 'Tafo', district: 'Old Tafo', lat: 6.735, lng: -1.605 },

  // Takoradi / Western Deep Localities
  { name: 'Takoradi Market Circle North', suburb: 'Market Circle', district: 'Sekondi-Takoradi Metro', lat: 4.886, lng: -1.756 },
  { name: 'Sekondi Commercial Bank Street', suburb: 'Sekondi Town', district: 'Sekondi-Takoradi', lat: 4.935, lng: -1.706 },
  { name: 'Effiakuma Police Station Area', suburb: 'Effiakuma', district: 'Effia Municipal', lat: 4.907, lng: -1.762 },

  // Tamale / Northern Deep Localities
  { name: 'Tamale Central Hospital Area', suburb: 'Tamale Central', district: 'Tamale Metropolitan', lat: 9.409, lng: -0.852 },
  { name: 'UDS Campus Road, Tamale', suburb: 'Dungu / UDS', district: 'Tamale South', lat: 9.360, lng: -0.865 },

  // Cape Coast / Central Deep Localities
  { name: 'Cape Coast Castle Square', suburb: 'Castle Area', district: 'Cape Coast Metropolitan', lat: 5.103, lng: -1.244 },
  { name: 'UCC Science Complex Area', suburb: 'UCC Main Campus', district: 'Cape Coast North', lat: 5.120, lng: -1.287 },

  // Volta / Eastern / Bono Hubs
  { name: 'Ho Market Square', suburb: 'Ho Central', district: 'Ho Municipal', lat: 6.602, lng: 0.472 },
  { name: 'Koforidua Jackson Park Area', suburb: 'Koforidua Central', district: 'New Juaben South', lat: 6.084, lng: -0.259 },
  { name: 'Sunyani Post Office Square', suburb: 'Sunyani Central', district: 'Sunyani Municipal', lat: 7.336, lng: -2.328 },
];

// Ultra-fast instant IP Geolocation Engine
export async function fetchInstantIpLocation() {
  try {
    const res = await fetch('https://freeipapi.com/api/json', { cache: 'no-store' });
    const data = await res.json();
    if (data && typeof data.latitude === 'number' && typeof data.longitude === 'number') {
      return {
        lat: data.latitude,
        lng: data.longitude,
        cityName: data.cityName || 'Ghana',
        regionName: data.regionName || 'Ghana',
        countryName: data.countryName || 'Ghana',
        isIp: true,
      };
    }
  } catch {
    try {
      const res2 = await fetch('https://ipapi.co/json/', { cache: 'no-store' });
      const data2 = await res2.json();
      if (data2 && typeof data2.latitude === 'number' && typeof data2.longitude === 'number') {
        return {
          lat: data2.latitude,
          lng: data2.longitude,
          cityName: data2.city || 'Ghana',
          regionName: data2.region || 'Ghana',
          countryName: data2.country_name || 'Ghana',
          isIp: true,
        };
      }
    } catch {
      // Ignore
    }
  }
  return null;
}

// Haversine formula for exact distance in meters
export function getDistanceMeters(lat1, lon1, lat2, lon2) {
  if (typeof lat1 !== 'number' || typeof lon1 !== 'number' || typeof lat2 !== 'number' || typeof lon2 !== 'number') {
    return 0;
  }
  const R = 6371000; // Radius of Earth in meters
  const dLat = (lat2 - lat1) * (Math.PI / 180);
  const dLon = (lon2 - lon1) * (Math.PI / 180);
  const a =
    Math.sin(dLat / 2) * Math.sin(dLat / 2) +
    Math.cos(lat1 * (Math.PI / 180)) *
      Math.cos(lat2 * (Math.PI / 180)) *
      Math.sin(dLon / 2) *
      Math.sin(dLon / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return Math.round(R * c);
}

// Haversine formula for exact distance in KM
export function getDistanceKm(lat1, lon1, lat2, lon2) {
  const meters = getDistanceMeters(lat1, lon1, lat2, lon2);
  return meters / 1000;
}

// Find closest deep micro-locality fallback
export function findNearestMicroLocality(lat, lng) {
  let closest = null;
  let minMeters = Infinity;

  for (const item of GHANA_MICRO_LOCALITIES) {
    const meters = getDistanceMeters(lat, lng, item.lat, item.lng);
    if (meters < minMeters) {
      minMeters = meters;
      closest = item;
    }
  }

  if (closest && minMeters <= 8000) {
    return {
      landmark: closest.name,
      suburb: closest.suburb,
      district: closest.district,
      distanceMeters: minMeters,
    };
  }

  return {
    landmark: `GPS Coordinates (${lat.toFixed(5)}, ${lng.toFixed(5)})`,
    suburb: `Ghana (${lat.toFixed(3)}, ${lng.toFixed(3)})`,
    district: 'Ghana',
    distanceMeters: 0,
  };
}

// Deep Multi-API Reverse Geocoding with Street & Building Level Precision
export async function reverseGeocodeGhanaDeep(lat, lng) {
  try {
    const res = await fetch(
      `https://nominatim.openstreetmap.org/reverse?format=json&lat=${lat}&lon=${lng}&zoom=19&addressdetails=1`,
      { headers: { 'User-Agent': 'NetworkRadarGhana/2.0' } }
    );
    const data = await res.json();
    const addr = data?.address || {};

    const street =
      addr.road ||
      addr.street ||
      addr.pedestrian ||
      addr.path ||
      addr.footway ||
      addr.amenity ||
      addr.building ||
      addr.commercial ||
      addr.shop;

    const suburb =
      addr.suburb ||
      addr.neighbourhood ||
      addr.residential ||
      addr.quarter ||
      addr.village ||
      addr.city_district ||
      addr.town;

    const city = addr.city || addr.town || addr.county || 'Ghana';
    const district = addr.county || addr.state_district || addr.state || city;

    if (street || suburb) {
      const landmarkText = street
        ? suburb
          ? `${street}, ${suburb}`
          : `${street}, ${city}`
        : `${suburb}, ${city}`;

      return {
        landmark: landmarkText,
        suburb: suburb || city,
        district: district,
        fullAddress: data.display_name,
        confidence: 'HIGH_GPS_PRECISION',
      };
    }
  } catch {
    // Fallback to local deep micro-locality matching
  }

  const fallback = findNearestMicroLocality(lat, lng);
  return {
    landmark: fallback.landmark,
    suburb: fallback.suburb,
    district: fallback.district,
    fullAddress: `${fallback.landmark}, ${fallback.district}`,
    confidence: 'MICRO_LOCALITY_MATCH',
  };
}
